apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qaq.fullname" . }}-test-connection"
  labels:
    {{- include "qaq.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qaq.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
